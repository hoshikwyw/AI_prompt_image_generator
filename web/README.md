# web

The app. See the [project README](../README.md) and [HISTORY.md](../HISTORY.md) for context.

```bash
cp .env.example .env.local   # CF_ACCOUNT_ID + CF_API_TOKEN is enough (free tier)
npm install
npm run dev
```
