# web

The app. See the [project README](../README.md) and [HISTORY.md](../HISTORY.md) for context.

```bash
npm install
npm run dev
```

No keys needed — the collection is a local JSON file, seeded on first run.
`.env.example` covers the image providers, which are dormant until a key exists.
